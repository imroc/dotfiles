# 外观
# JetBrainsMono 作为默认字体，中文 fallback 到等距的更纱黑体，
# 以便在终端看含中文的 ASCII 图时能够对齐
font-family = JetBrainsMono Nerd Font
font-family = Sarasa Mono SC
font-size = 14
background-opacity = 1
# 强制 ambiguous width 的几何符号用主字体渲染（单宽），
# 避免 fallback 到更纱黑体导致双宽，影响 nvim render-markdown 等插件的对齐
font-codepoint-map = U+25CB,U+25CF,U+25C6,U+25C7=JetBrainsMono Nerd Font
theme = TokyoNight Night
cursor-style = block

# split (pane)
keybind = super+enter=toggle_split_zoom
keybind = super+h=goto_split:left
keybind = super+j=goto_split:down
keybind = super+k=goto_split:up
keybind = super+l=goto_split:right
split-inherit-working-directory = true
split-preserve-zoom = navigation
# split-divider-color = #F0f0f0
split-divider-color = #999999

# Shift+Ejnter 发送换行符（解决 Zellij 下 Shift+Enter 无法换行的问题）
# keybind = shift+enter=text:\x1b[200~\n\x1b[201~
custom-shader = shaders/bloom025.glsl
custom-shader = shaders/cursor_blaze_no_trail.glsl
custom-shader = shaders/cursor_smear.glsl
window-padding-x = 4,2
window-padding-y = 6,0
confirm-close-surface = true
clipboard-paste-protection = false
macos-icon = official
macos-titlebar-style = transparent
unfocused-split-opacity = 0.6
unfocused-split-fill = #0d0e14

# quick terminal
keybind = global:cmd+/=toggle_quick_terminal
quick-terminal-position = center
quick-terminal-size = 80%,80%
quick-terminal-screen = main
quick-terminal-animation-duration = 0
# quick-terminal-autohide = false

# tab
keybind = super+n=new_tab
keybind = super+]=next_tab
keybind = super+[=previous_tab
keybind = super+shift+]=move_tab:1
keybind = super+shift+[=move_tab:-1
keybind = super+t=activate_key_table:tab
keybind = super+shift+r=prompt_tab_title
keybind = chain=deactivate_key_table
keybind = tab/r=prompt_tab_title
keybind = chain=deactivate_key_table
keybind = tab/x=close_tab
keybind = tab/escape=deactivate_key_table
keybind = tab/enter=deactivate_key_table
keybind = tab/catch_all=ignore
tab-inherit-working-directory = true

# resize
keybind = resize/k=resize_split:up,100
keybind = resize/j=resize_split:down,100
keybind = resize/h=resize_split:left,100
keybind = resize/l=resize_split:right,100
keybind = resize/escape=deactivate_key_table
keybind = resize/enter=deactivate_key_table
keybind = resize/e=deactivate_key_table
keybind = chain=equalize_splits
keybind = resize/catch_all=ignore
keybind = super+ctrl+r=activate_key_table:resize

# scroll
keybind = super+u=scroll_page_up
keybind = chain=activate_key_table:scroll
keybind = scroll/k=scroll_page_lines:-3
keybind = scroll/j=scroll_page_lines:3
keybind = scroll/b=scroll_page_up
keybind = scroll/f=scroll_page_down
keybind = scroll/super+u=scroll_page_fractional:-0.5
keybind = scroll/u=scroll_page_fractional:-0.5
keybind = scroll/e=scroll_page_fractional:-0.5
keybind = scroll/d=scroll_page_fractional:0.5
keybind = scroll/g=scroll_to_top
keybind = scroll/shift+g=scroll_to_bottom
keybind = scroll/super+enter=toggle_split_zoom
keybind = scroll/super+h=goto_split:left
keybind = scroll/super+j=goto_split:down
keybind = scroll/super+k=goto_split:up
keybind = scroll/super+l=goto_split:right
keybind = scroll/space=deactivate_key_table
keybind = chain=scroll_to_bottom
keybind = scroll/i=deactivate_key_table
keybind = chain=scroll_to_bottom
keybind = scroll/escape=deactivate_key_table
keybind = chain=scroll_to_bottom
keybind = scroll/enter=deactivate_key_table
keybind = chain=scroll_to_bottom
keybind = scroll/super+s=deactivate_key_table
keybind = scroll/catch_all=ignore
keybind = super+s=activate_key_table:scroll
# scroll mode 中 Cmd+I: 导出屏幕内容到临时文件 → 右侧新 split → 退出 scroll mode
# 新 split 启动 fish 时钩子 (ghostty-scrollback.fish) 检测剪贴板中的文件路径，自动用 nvim 打开
# 注意: cmux 环境中此绑定不会触发（Karabiner 先拦截了 Cmd+I）
keybind = scroll/super+i=write_screen_file:copy
keybind = chain=new_split:right
keybind = chain=deactivate_key_table

# window
keybind = super+shift+n=new_window

# 修复微信输入法导致终端 IRM 被切换为覆盖模式的问题（Cmd+Shift+I 重置）
# keybind = super+shift+i=text:\x1b[4l

# 其它
scrollback-limit = 10000000
# 禁用通知，AI 工具的状态用 Vibe Island 统一监控和通知
# desktop-notifications = false
# ── scrollback copy mode ──────────────────────────────────────────────
# 用 nvim 打开终端屏幕内容，供用户自由选择复制文本
#
# cmux 环境 (主要使用):
#   Cmd+I 由 Karabiner 拦截 (条件: frontmost app = com.cmuxterm.app)
#   执行 ~/.local/bin/cmux-scrollback-copy 脚本:
#     cmux capture-pane → 临时文件 → 同 pane 新建 tab → nvim 打开
#   此处的 ghostty keybind 不会触发（Karabiner 优先级更高）
#
# ghostty 原生环境:
#   Cmd+I 由以下 ghostty keybind 处理:
#     write_screen_file:copy — 屏幕内容写入临时文件，路径存入剪贴板
#     chain=new_split:right  — 右侧新建 split
#   新 split 启动 fish 时钩子 (ghostty-scrollback.fish) 检测剪贴板中的临时文件路径
#   自动调用 ghostty-scrollback-copy 用 nvim 打开，并用 osascript 模拟 Cmd+Enter 最大化 split
#   注意: ghostty 无法像 cmux 那样在新 tab（而非 split）中打开，因为 ghostty 没有
#   CLI/socket API 来查询聚焦状态、新建 tab、发送命令等，只能通过 keybind chain 组合
#   内置 action，new_split:right + fish 钩子是 ghostty 原生环境下的最优解
# ─────────────────────────────────────────────────────────────────────
keybind = super+i=write_screen_file:copy
keybind = chain=new_split:right
macos-option-as-alt = true

