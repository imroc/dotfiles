enabled_layouts tall,stack
layout tall
cd ~/dev/kubernetes-manifests
launch --title "manifests"

new_tab
enabled_layouts tall,grid,stack
layout tall
cd ~
launch --title "kubectl"

focus
focus_os_window
